#include "sim.h"

SIM::SIM(void)
{
    // SNR_START = 2.0;      // Start SNR for simulation
    // SNR_END = 6;       // Stop SNR for simulation
    // SNR_STEP = 0.5;       // SNR step for simulation
    // Channel = "Linear";   // Channel modelling
    // Noise = "AWGN";       // Noise modelling
    // Mod = "BPSK";         // Modulation technique
    // MAX_FRAMES = 5000000; // Maximum simulated frames
    // MIN_FRAMES = 10000;   // Minimum simulated frames
    // MIN_ERRORS = 50;     // Minimum error frames
    // STABLE_ERRORS = 5;    // Stable errors if exceed MAX_FRAMES
    // TARGET_FER = 1e-4;    // Target FER
    // FC = 0;               // Frame counts
    // FEC = 0;              // Frame error counts
    // BEC = 0;              // Bit error counts
    // FER = 0;              // Frame error rate
    // BER = 0;              // Bit error rate
    // TP = 0;               // Decoding through puts
    // AvgT = 0;             // Average number of decoding attempts
}